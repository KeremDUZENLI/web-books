### Exercise 3.3

The Framework-3 `SimpleScene` includes a default light. Locate the lighting code in `SetupScene`.

Comment out the existing light and add an `AmbientLight` with a color of `Colors.Gray`. Run the program. How does the sphere look? Why does it look 2D?
