### Answer 3.6

**Construction Steps (Pseudocode/Math):**
Assume unit spheres (radius 1, diameter 2).

1.  **Base:**
    * Scale: $3.0$ (Radius 3, Height 6).
    * Translation: $(0, 0, 0)$. Top is at $y=3$.

2.  **Torso:**
    * Scale: $2.0$ (Radius 2, Height 4).
    * Position Calculation: Sit on top of base ($y=3$) + radius of torso ($y=2$).
    * Translation: $(0, 5, 0)$. Top is at $y=3+4=7$.

3.  **Head:**
    * Scale: $1.0$ (Radius 1, Height 2).
    * Position Calculation: Sit on top of torso ($y=7$) + radius of head ($y=1$).
    * Translation: $(0, 8, 0)$.

**Transforms:**
Use `TranslateTransform3D` applied to the `ModelVisual3D` of each sphere.