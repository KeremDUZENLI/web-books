### Exercise 4.3

Add a new property to the scene to control the horizontal position of the robot.

1.  Define a variable `_torsoX` in the class.
2.  In `OnUpdate`, assign `_torsoX` to the `OffsetX` property of the torso's transform.
3.  Add a slider to the Tweak Bar to control `_torsoX`.
