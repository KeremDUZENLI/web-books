### Answer 4.5
**Implementation:**
1.  **Light Setup:**
    ```csharp
    PointLight pointLight = new PointLight(Colors.Red, new Point3D(0, 5, 0));
    sceneGroup.Children.Add(pointLight);
    ```
2.  **Variables:** `private double _lx, _ly, _lz;`
3.  **Update Logic:**
    ```csharp
    // Inside OnUpdate
    pointLight.Position = new Point3D(_lx, _ly, _lz);
    ```
4.  **Observation:**
    As the red light moves closer to the robot, surfaces facing the light turn reddish. Where the red light overlaps with the existing white light, the colors add up (e.g., White + Red stays tinted, shadows become colored).