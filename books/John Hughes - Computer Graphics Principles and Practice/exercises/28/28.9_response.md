### Answer 28.9

Given $L^*, a^*, b^*$ and reference white $X_w, Y_w, Z_w$.

1.  **Solve for Y:**
    From $L^* = 116 (Y/Y_w)^{1/3} - 16$:
    Let $f_Y = (L^* + 16) / 116$.
    If $L^* > \kappa \cdot \epsilon$ (standard threshold), $Y = Y_w \cdot f_Y^3$.
    (Otherwise use linear segment inversion).

2.  **Solve for X and Z:**
    From $a^* = 500 (f(X/X_w) - f(Y/Y_w))$:
    $f(X/X_w) = \frac{a^*}{500} + f(Y/Y_w) = \frac{a^*}{500} + f_Y$.
    Let $f_X = \frac{a^*}{500} + f_Y$.
    $X = X_w \cdot f_X^3$ (assuming $f_X^3 > 0.008856$).

    From $b^* = 200 (f(Y/Y_w) - f(Z/Z_w))$:
    $f(Z/Z_w) = f(Y/Y_w) - \frac{b^*}{200} = f_Y - \frac{b^*}{200}$.
    Let $f_Z = f_Y - \frac{b^*}{200}$.
    $Z = Z_w \cdot f_Z^3$ (assuming condition met).

**Final ## Formulas (High Luminance):**
$Y = Y_w \left( \frac{L^*+16}{116} \right)^3$
$X = X_w \left( \frac{a^*}{500} + \frac{L^*+16}{116} \right)^3$
$Z = Z_w \left( \frac{L^*+16}{116} - \frac{b^*}{200} \right)^3$
