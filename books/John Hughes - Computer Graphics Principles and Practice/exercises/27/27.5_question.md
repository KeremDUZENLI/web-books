### Exercise 27.5
    
Plot $F_{R}$, the Fresnel reflectance, as a function of $\theta_{i}$, for an interface where $r=n_{1}/n_{2}$ is 1, 1.5, 2, and 3. To do so you'll need to compute $\theta_{t}$ using Snell's law.