### Answer 12.6
**Note:** There is a typo in the provided source text for Exercise 12.6 ($P_1=P_2=(1,1)$). Assuming standard unit square to generic quad mapping or distinct points. Based on the coordinates provided:
$P_1=(1,1), P_2=(1,1)$ (Degenerate input).
Assuming the text meant $P_2=(-1,1)$ or similar to form a square/quad.
*Correction based on typical context:* $P_1=(1,1), P_2=(-1,1), P_3=(-1,-1), P_4=(1,-1)$.
Target: $Q_1=(0.5, 0.5), Q_2=(-1,1), Q_3=(0.5, -0.5), Q_4=(1,-1)$.

**Step 1: Map $P$ to Unit Square**
Since $P_i$ are corners of square centered at origin size 2.
Scale by 0.5, then translate...
Actually, just solve for matrix M mapping lines.
Notice $Q_2=P_2$ and $Q_4=P_4$. Points 2 and 4 are fixed.
$P_1(1,1) \to Q_1(0.5, 0.5)$. (Scaled by 0.5).
$P_3(-1,-1) \to Q_3(0.5, -0.5)$. (X flipped? Y scaled?)

**Inspection:**
$x' = x$ ? No. $1 \to 0.5$, $-1 \to -1$.
$y' = y$ ? No. $1 \to 0.5$, $-1 \to 1$ ($Q_2$), $-1 \to -0.5$.

*Note: Without valid distinct points for $P_1, P_2, P_3, P_4$, a unique homography cannot be determined. The provided text lists $P_1=(1,1)$ and $P_2=(1,1)$. This is technically impossible to solve as a projective transform between two quadrilaterals.*