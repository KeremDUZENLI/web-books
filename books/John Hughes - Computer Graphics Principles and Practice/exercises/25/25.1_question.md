### Exercise 25.1
    
Our algorithm for computing the boundary of a triangle mesh involved iterating through all the faces, and it runs in $O(f)$ time. Describe a connected triangle mesh with $O(f)$ boundary edges, thus showing that the $O(f)$ runtime is as good as possible.