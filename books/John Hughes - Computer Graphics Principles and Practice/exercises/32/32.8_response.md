### Answer 32.8

**Problem:** Path tracing traditionally ignores point lights for primary (eye) rays because the probability of a random ray hitting a mathematical point is zero. This prevents point lights from being directly visible (they don't appear as bright spots in the image).

**Solution:**
Add a specific rendering pass or a check within the main loop for "direct visibility of point lights".

1. **Iterate:** For every pixel ray (eye ray).
2. **Check:** For each point light source  at position :
* Compute vector  (Eye).
* Check if the current pixel ray direction  aligns with  within a small tolerance (if representing the light as a small sphere) or exactly (if rasterizing a marker).
* *Better Physical Approach (Bidirectional idea):* Connect the light to the eye.
* Project the point light position  onto the image plane.
* Check if the projected position  falls within the current pixel boundaries.
* Check visibility: Cast a ray from Eye to . If unoccluded, add the light's emitted radiance (scaled by  and any sensor response function) to the pixel .





**Implementation Detail:**
This is effectively "splatting" the light onto the image. In a ray tracing context, you check if the point light projects onto the current pixel. If `is_visible(Eye, LightPos)`, add `LightPower / dist^2` to the pixel color.