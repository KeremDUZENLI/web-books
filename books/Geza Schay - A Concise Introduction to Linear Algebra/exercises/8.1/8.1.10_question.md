### Exercise 8.1.10

For five instances of `A = round(10*rand(4,5))` find an LU factorization of $A$, using `[L,U,P] = lu(A)`, and change it, using the `diag` command, to the corresponding LDU factorization. Check that $LDU=PA$ holds.
