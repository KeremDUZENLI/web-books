### Answer 3.5.14

**Proof:**
$Col(AB) = A(Col(B))$.
Since $A$ is nonsingular (invertible), the linear map $T(\mathbf{v}) = A\mathbf{v}$ is an isomorphism (bijective linear map).
Isomorphisms preserve dimension.
Thus $dim(Col(AB)) = dim(Col(B))$.
$Rank(AB) = Rank(B)$.