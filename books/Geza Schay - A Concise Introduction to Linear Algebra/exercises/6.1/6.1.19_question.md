### Exercise 6.1.19

The following program achieves the same result as the one above but in a much more efficient way. Enter it and explain what it does:

```matlab
n = 6;
x = randperm(n);
I = eye(n);
P = I;
for i = 1:n
    P(i,:) = I(x(i),:);
end
det(P)
```