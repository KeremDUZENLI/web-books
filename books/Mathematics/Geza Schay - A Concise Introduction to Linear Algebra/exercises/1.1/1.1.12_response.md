### Answer 1.1.12

**a. `u + v`:** Element-wise addition. Result: `[2 -5 8 5]`.
**b. `2*u`:** Scalar multiplication. Result: `[2 -6 12 0]`.
**c. `2*u`:** (Assuming no syntax difference in context) Same as above.
**d. `s = u/2`:** Scalar division. Result: `[0.5 -1.5 3 0]`.
**e. `u/v`:** Matrix division (solving system $x\mathbf{v} = \mathbf{u}$ in least squares). Returns a scalar approx $0.6$ (roughly).
**f. `u./v`:** Element-wise division. Result: `[1 1.5 3 0]`.
**g. `u(2)`:** Accesses 2nd element. Result: `-3`.
**h. `t = u/u(2)`:** Divides vector by scalar -3. Result: `[-0.333 1 -2 0]`.
**i. `format rat; s`:** Displays `s` as fractions. `[1/2 -3/2 3 0]`.
**j. `t`:** Displays `t` as fractions. `[-1/3 1 -2 0]`.
**k. `format short; s`:** Reverts to 4 decimal places.
**l. `s = 3:8`:** Creates vector `[3 4 5 6 7 8]`.
**m. `t = 1:0.2:2.8`:** Creates vector from 1 to 2.8 with step 0.2. `[1.0 1.2 ... 2.8]`.
**n. `length(t)`:** Returns number of elements. 10.