### Answer 7.1.20

**MATLAB Check (Exercise 7.1.5 matrix):**
$A = [2, 0, 1; 0, 2, 0; 1, 0, 2]$.
1.  `c = poly(A)` returns characteristic polynomial coefficients: `[1, -6, 11, -6]`. ($\lambda^3 - 6\lambda^2 + 11\lambda - 6$).
2.  `d = roots(c)` returns `[3; 2; 1]`. Correct.
3.  `eig(A)` returns `[1; 2; 3]` and matrix of vectors.
    * $\lambda=1 \implies \mathbf{v} \propto (1, 0, -1)$.
    * $\lambda=2 \implies \mathbf{v} \propto (0, 1, 0)$.
    * $\lambda=3 \implies \mathbf{v} \propto (1, 0, 1)$.
Results match hand calculations.