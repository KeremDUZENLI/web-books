### Answer 5.1.26

**Code:**
```matlab
x = [1; 2; -1; -2; 4];
y = [2; 4; 0; 5; 14];
p = polyfit(x, y, 2); % Degree 2 for parabola
% p is [a, b, c] for ax^2 + bx + c
xx = linspace(min(x), max(x), 100);
yy = polyval(p, xx);
plot(x, y, 'o', xx, yy, '-');
```