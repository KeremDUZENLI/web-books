### Answer 7.2.13

**Matrix:** $A = \begin{bmatrix} 1.1 & 0 \\ 0.2 & 0.6 \end{bmatrix}$.

**a. Verification:** Use MATLAB `eig(A)` or multiply. $\lambda_1 = 1.1, \lambda_2 = 0.6$.
Long run behavior is dominated by $\lambda_1 = 1.1$ (Growth).

**b. Experiment:**
Change entry $A_{22}$ (survival of older) or $A_{12}$ (reproduction).
If the maximum eigenvalue drops below 1, the population faces extinction.
A steady population requires $\lambda_{max} = 1$.

**c. Steady State:**
Solve $\det(A(\theta) - I) = 0$ for the parameter.