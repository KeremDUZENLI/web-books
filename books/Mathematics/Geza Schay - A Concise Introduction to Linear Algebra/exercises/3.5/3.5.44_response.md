### Answer 3.5.44

**MATLAB `magic(4)`:**
Rank is 3.
a. `rref` on $A, A^T$ to get $B, C$.
b. `null` gives orthonormal basis. Spans same space.
c. Decompose $x$: Project onto $Row(A)$ basis (orthonormal preferred).
d. `null(A')` for Left Null.