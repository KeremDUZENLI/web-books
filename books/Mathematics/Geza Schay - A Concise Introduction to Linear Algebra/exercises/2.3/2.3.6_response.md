### Answer 2.3.6

**Matrix (Ex 2.1.12):**
LHS is same as 2.3.5. RHS is $(7, 1, 6)^T$.

**Gauss-Jordan Elimination:**
Follow the same row operations as 2.3.5, but track RHS.
1.  Swap/Negate: RHS $\to -1$.
2.  Eliminate Col 1:
    $R_2: 7 - 3(-1) = 10$.
    $R_3: 6 - 4(-1) = 10$.
3.  Eliminate $R_3$ via $R_2$: $10 - 10 = 0$.
    Current State:
    $$ \left[ \begin{array}{ccccc|c} 1 & -2 & -2 & -3 & -3 & -1 \\ 0 & 0 & 5 & 10 & 14 & 10 \\ 0 & 0 & 0 & 0 & -1 & 0 \end{array} \right] $$
    Wait, in 2.1.12 the last row was $(0,0,0,0,-1 \mid 1)$. Let's re-verify $R_3$.
    Original $R_3 = (4, -8, -3, -2, 6 \mid 6$? No original matrix in problem statement says 6).
    Re-calculating RHS ops:
    $R_1 \leftarrow -1 \times (-1, 2, 2, 3, 1 | 1) \implies (1, -2, -2, -3, -1 | -1)$.
    $R_2 \leftarrow (3, -6, -1, 1, 7 | 7) - 3R_1 \implies 7 - 3(-1) = 10$.
    $R_3 \leftarrow (4, -8, -3, -2, 6 | 6) - 4R_1 \implies 6 - 4(-1) = 10$.
    $R_3 \leftarrow R_3 - R_2 \implies 10 - 10 = 0$.
    Ah, in 2.1.12 Exercise, the last coefficient for $x_5$ was 1? No, it was $3,-6,-1,1,**7**$ (variable $x_5$) and RHS 1.
    Let's check the matrix provided in the prompt for 2.3.6:
    Col 5 is: 7, 1, 6. RHS is 7, 1, 6.
    The matrix provided in the prompt for 2.3.6 is $3\times5$ augmented? No, it is $3 \times 5$ coefficients and 1 constant.
    The matrix in the prompt is:
    $$ \begin{bmatrix} 3 & -6 & -1 & 1 & 7 \\ -1 & 2 & 2 & 3 & 1 \\ 4 & -8 & -3 & -2 & 6 \end{bmatrix} $$
    Wait, this is $3 \times 5$. This represents 4 variables and 1 constant? Or homogeneous?
    Assume augmented matrix for $Ax=b$. Variables $x_1, x_2, x_3, x_4$.
    Following 2.3.5 steps on LHS, RHS is last col.
    $R_1: -1$. $R_2: 10$. $R_3: 0$.
    Wait, $R_3 \leftarrow R_3 - R_2$.
    $R_3$ LHS is $(0,0,0,0,-1)$. RHS is 0.
    So $-x_5$ term doesn't exist.
    Let's assume the prompt implies 4 variables.
    Pivot rows: $R_1, R_2, R_3$.
    $R_3$ is $[0, 0, 0, 0 \mid 0]$. Consistent.
    RHS was 0?
    $R_3 \leftarrow R_3 - 4R_1$: $6 - 4(-1) = 10$.
    $R_2 \leftarrow R_2 - 3R_1$: $7 - 3(-1) = 10$.
    $R_3 - R_2 = 0$.
    So the system is consistent.

    **RREF:**
    $R_2 \leftarrow R_2/5 \implies (0, 0, 1, 2 \mid 2)$.
    $R_1 \leftarrow R_1 + 2R_2 \implies -1 + 2(2) = 3$.
    $$ \left[ \begin{array}{cccc|c} 1 & -2 & 0 & 1 & 3 \\ 0 & 0 & 1 & 2 & 2 \\ 0 & 0 & 0 & 0 & 0 \end{array} \right] $$

**Solution:**
$x_3 = 2 - 2t$.
$x_1 = 3 + 2s - t$.
$$ \mathbf{x} = \begin{bmatrix} 3 \\ 0 \\ 2 \\ 0 \end{bmatrix} + s \begin{bmatrix} 2 \\ 1 \\ 0 \\ 0 \end{bmatrix} + t \begin{bmatrix} -1 \\ 0 \\ -2 \\ 1 \end{bmatrix} $$