### Answer 2.8

**(a) Minute Hand Animation:**

- **Transform:** Add `<RotateTransform x:Name="ActualTimeMinute" Angle="0"/>`.
- **Animation:** Add `DoubleAnimation` targeting `ActualTimeMinute`.
- **Duration:** 1 hour per revolution `Duration="01:00:00"`.

**(b) Second Hand Animation:**

- **Duration:** 1 minute per revolution `Duration="00:01:00"`.

**(c) Hour Hand Repair:**

- **Issue:** The text mentions the hour hand moves "unnaturally fast".
- **Fix:** Set `Duration="12:00:00"` (12 hours for a full 360 rotation).

**(d) Demo Initialization:**

- Manually calculate offsets (e.g., if local time is 3:00, set Hour Hand base angle to 90).

**(e) Procedural Logic:**

- In C# code-behind: `DateTime.Now` to get current time.
- Calculate angles: `(Hours % 12 + Minutes/60.0) * 30` for hour hand.
- Set `RotateTransform.Angle` properties programmatically on load.
