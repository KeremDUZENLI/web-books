### Answer 32.7


**(a) & (b) Implementation Sketch:**

* **Triangle Point Generation:**
1. Generate random .
2. If , transform: , .
3. .


* **Mesh Sampling:**
1. Compute area of every triangle . Total area .
2. Construct Cumulative Distribution Function (CDF) array: .
3. To sample: Pick . Find  such that . Sample point in .



**(c) Faster Selection ():**
Use **binary search** on the cumulative area array (CDF). Since the array is sorted (monotonically increasing), finding the index  takes  steps.
Alternatively, use the **Alias Method** (Vose's Alias Method) to select a triangle in  time, though this requires  preprocessing time.

**(d) Sorting by Size:**
**Impact:**

* If using linear search: Yes, sorting helps significantly if area variance is high (a few large triangles cover most of the area). The expected search depth decreases.
* **Typical Graphics Model:** Graphics models often consist of fairly uniform tessellations (micropolygons, regular grids). While there is variation, it is rarely extreme enough (orders of magnitude) across the *entire* mesh to make linear search competitive with binary search.
* **Recommendation:** Even with sorting, linear search is . Binary search () is preferred regardless of sorting.