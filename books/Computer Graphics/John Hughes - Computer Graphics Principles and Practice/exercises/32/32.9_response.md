### Answer 32.9


**(a) Approximation Conditions:**
We approximate the integral over the area light by a single term:
.
This is the "point source" approximation.
The error comes from the variation of:

1. Distance  over the light source.
2. Cosine terms () over the light source.

**Rule of Thumb (5x Rule):**
The approximation is generally considered accurate (within ) if the distance  is at least **5 times** the maximum extent (radius ) of the light source ().
Specifically, the solid angle  is the leading term. The variation behaves roughly as . If , error is small.

**(b) Bounded BRDF Variation:**
If the BRDF  varies slowly (), we can bound the error introduced by assuming a constant BRDF over the solid angle of the light.
The error will depend on the product of the light's angular size () and the Lipschitz constant .
If the light subtends a small angle (large , small ), the BRDF is nearly constant over the integration domain. The condition for 1% accuracy would involve . This allows us to treat the BRDF as constant for that light interaction, simplifying the integral to just the irradiance calculation.