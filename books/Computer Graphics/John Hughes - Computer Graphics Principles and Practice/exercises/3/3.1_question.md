### Exercise 3.1

Launch the lab for this section. The C# code that configures the scene is in the file `SimpleScene.cs`. Locate the `SetupScene` method and find the lines that configure the camera.

Experiment with changing the eye position. What happens if you move it to $(5, 0, 10)$? To $(0, 0, 5)$? To $(0, 0, -10)$?
