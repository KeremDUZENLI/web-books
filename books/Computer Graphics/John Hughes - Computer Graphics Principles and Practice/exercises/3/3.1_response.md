### Answer 3.1

**(a)**
Iterate over every 2D coordinate $(x, y)$. 
`Depth = min(Depth_{lute}(x,y), Depth_{candlestick}(x,y))`
Draw the pixel color associated with the object that has the minimum depth at that coordinate.

**(b)**
- Depth of field (blurring pixels based on distance from the focal plane).
- Fog and atmospheric attenuation (blending the pixel color with a background color based on depth).
- Shadow mapping (comparing camera depth to light-source depth to determine occlusion).