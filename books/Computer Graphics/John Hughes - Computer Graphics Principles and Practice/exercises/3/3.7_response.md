### Answer 3.7

For a rotation around the Y-axis:
```python
t = t + 0.05
for v in vertices:
    x_new = v.x * cos(0.05) - v.z * sin(0.05)
    z_new = v.x * sin(0.05) + v.z * cos(0.05)
    v.x = x_new
    v.z = z_new
render()
```