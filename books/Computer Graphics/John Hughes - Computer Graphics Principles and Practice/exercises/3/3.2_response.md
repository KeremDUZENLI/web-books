### Answer 3.2

**Draw dots after segments:**
Reorder the rendering functions. Execute the loop that iterates through the edge list (drawing line segments) first. Then, execute the loop that iterates through the vertex list (drawing points). The points will overwrite the lines in the framebuffer.

**Do not draw dots at all:**
Remove or comment out the loop that iterates through the vertex list entirely.