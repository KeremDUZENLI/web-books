### Exercise 4.2

Locate the definition of the `Torso` object in the source code.

Verify that its transform is a `TranslateTransform3D`. Explain why modifying the `OffsetY` property of this single transform moves the entire robot (head, arms, and legs) rather than just the torso cube.
