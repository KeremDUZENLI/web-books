### Answer 4.3

```csharp
// Logic outline:
private int currentIndex = 0;
private List<BitmapSource> images = LoadImages();

private void OnButtonClick(object sender, EventArgs e) {
    currentIndex = (currentIndex + 1) % images.Count;
    MyImageControl.Source = images[currentIndex];
}

```

Store image references in an array or list. Use a modulo operator to cycle through indices when the button click event fires, and re-assign the `.Source` property of the UI image element.