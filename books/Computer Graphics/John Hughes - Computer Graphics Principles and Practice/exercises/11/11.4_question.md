### Exercise 11.4
In Listing 11.2, we have a condition "if $\theta$ is near $\pi$" which handles the special case of very-large-angle rotations by picking a nonzero column v of $M+I$ as the axis. If $\theta$ is not exactly $\pi$, then v will not quite be parallel to an axis. Explain why $v+Mv$ will be much more nearly parallel to the axis. Adjust the code in Listing 11.2 to apply this idea repeatedly to produce a very good approximation of the axis.