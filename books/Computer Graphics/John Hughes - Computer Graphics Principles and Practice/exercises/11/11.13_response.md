### Answer 11.13
**(a)** Incorrect. The new plane function is $\bar{f}(P) = f(T^{-1}(P))$. (Or transform normal by inverse transpose).
**(b)**
$g(t)$ is point on ray. $f(g(t))=0$ means ray hits plane.
$\bar{g}(t) = M^{-1}(A + tw) = M^{-1}A + t M^{-1}w$.
$\bar{f}(\bar{g}(t)) = f(T(\bar{g}(t))) = f(M(M^{-1}(A+tw))) = f(A+tw) = f(g(t))$.
The equation $f(g(t))=0$ is identical to $\bar{f}(\bar{g}(t))=0$ (using George's incorrect $\bar{f}$ definition with inverse ray?).
Correction: If we use the original $f$ (unit sphere) and inverse ray $\bar{g}$, we solve for intersections in the standard space.
**(c)**
1. Transform ray by $M^{-1}$: $A' = M^{-1}A, w' = M^{-1}w$.
2. Intersect ray $(A', w')$ with standard unit sphere.
3. Use the computed $t$ values for the original ray.
**(d)**
Let $n_{local}$ be the normal at the intersection point on the standard sphere.
$n_{world} = (M^{-1})^T n_{local}$.
Normalize $n_{world}$.