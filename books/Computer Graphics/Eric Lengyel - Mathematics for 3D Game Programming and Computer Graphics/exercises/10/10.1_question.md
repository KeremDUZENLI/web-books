### Exercise 10.1

Use a technique similar to that described in Section 9.1 to derive the (3,3) entry of a projection matrix based on the matrix $M_{infinite}^{\prime}$ given by Equation (10.10) that offsets depth values at a camera-space depth $P_{z}$ by a small amount $\delta$.
