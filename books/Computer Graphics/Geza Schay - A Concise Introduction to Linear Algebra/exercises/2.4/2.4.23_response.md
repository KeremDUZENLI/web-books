### Answer 2.4.23

**New Partition of Matrix 1 ($2 \times 2$ blocks, but different sizes):**
Row cut after row 2. Col cut after col 1.
$A_{11} = \begin{bmatrix} 1 \\ 3 \end{bmatrix}$ ($2 \times 1$).
$A_{12} = \begin{bmatrix} -2 & 1 & 0 \\ 4 & 0 & 1 \end{bmatrix}$ ($2 \times 3$).
$A_{21} = \begin{bmatrix} -1 \\ 0 \end{bmatrix}$ ($2 \times 1$).
$A_{22} = \begin{bmatrix} 0 & 0 & 0 \\ -1 & 0 & 0 \end{bmatrix}$ ($2 \times 3$).

**Corresponding Partition of Matrix 2:**
To define products $A_{ik} B_{kj}$, the row partitions of $B$ must match column partitions of $A$.
$A$ cols: 1, then 3.
So $B$ must have a row cut after row 1.
Columns of $B$ can be cut anywhere. Let's assume no cut (1 block wide) or keep 2/1 split.
Let's make $B$ $2 \times 1$ blocks.
$B_{11} = \begin{bmatrix} 1 & -2 & 0 \end{bmatrix}$ ($1 \times 3$).
$B_{21} = \begin{bmatrix} 2 & 0 & 0 \\ 0 & -3 & 1 \\ 0 & 2 & 3 \end{bmatrix}$ ($3 \times 3$).

**Compute:**
1.  **Top Block ($A_{11}B_{11} + A_{12}B_{21}$):**
    $A_{11}B_{11} = \begin{bmatrix} 1 \\ 3 \end{bmatrix} \begin{bmatrix} 1 & -2 & 0 \end{bmatrix} = \begin{bmatrix} 1 & -2 & 0 \\ 3 & -6 & 0 \end{bmatrix}$.
    $A_{12}B_{21} = \begin{bmatrix} -2 & 1 & 0 \\ 4 & 0 & 1 \end{bmatrix} \begin{bmatrix} 2 & 0 & 0 \\ 0 & -3 & 1 \\ 0 & 2 & 3 \end{bmatrix} = \begin{bmatrix} -4 & -3 & 1 \\ 8 & 2 & 3 \end{bmatrix}$.
    Sum: $\begin{bmatrix} -3 & -5 & 1 \\ 11 & -4 & 3 \end{bmatrix}$.

2.  **Bottom Block ($A_{21}B_{11} + A_{22}B_{21}$):**
    $A_{21}B_{11} = \begin{bmatrix} -1 \\ 0 \end{bmatrix} \begin{bmatrix} 1 & -2 & 0 \end{bmatrix} = \begin{bmatrix} -1 & 2 & 0 \\ 0 & 0 & 0 \end{bmatrix}$.
    $A_{22}B_{21} = \begin{bmatrix} 0 & 0 & 0 \\ -1 & 0 & 0 \end{bmatrix} B_{21} = \begin{bmatrix} 0 & 0 & 0 \\ -2 & 0 & 0 \end{bmatrix}$.
    Sum: $\begin{bmatrix} -1 & 2 & 0 \\ -2 & 0 & 0 \end{bmatrix}$.

**Result:**
$$ \begin{bmatrix} -3 & -5 & 1 \\ 11 & -4 & 3 \\ -1 & 2 & 0 \\ -2 & 0 & 0 \end{bmatrix} $$
(Note: Comparing to 2.4.22, the result is slightly different? Let's check 2.4.22 Top Left $C_{11}$.
In 2.4.22 we calculated $\begin{bmatrix} -3 & 0 \\ -1 & 2 \end{bmatrix}$.
Wait, row 2 col 1 of 2.4.22 result: $-1$. Here it is $11$.
Let's re-check $A_{12}B_{21}$ in 2.4.23.
$4(2) + 0(0) + 1(0) = 8$.
$3(1) + 8 = 11$.
Let's re-check 2.4.22 calculation.
$A_{11}B_{11} = \begin{bmatrix} -3 & -2 \\ -1 & 2 \end{bmatrix}$.
$A_{12}B_{21} = \begin{bmatrix} 0 & 1 \\ 0 & 0 \end{bmatrix} \begin{bmatrix} 0 & -3 \\ 0 & 2 \end{bmatrix} = \begin{bmatrix} 0 & 2 \\ 0 & 0 \end{bmatrix}$.
Sum: $\begin{bmatrix} -3 & 0 \\ -1 & 2 \end{bmatrix}$.
Why the difference?
Ah, the input matrices might be interpreted differently or my manual arithmetic has a slip.
Check 2.4.23 $A_{11}B_{11}$:
$3(1)=3$.
$A_{12}B_{21}$ Row 2 Col 1: $4(2) = 8$.
$3+8=11$.
Check 2.4.22 Row 2 Col 1: $-1 + 0 = -1$.
Let's check the original matrix product directly.
Row 2 of first matrix: $[-1, 0, 0, 0]$.
Col 1 of second matrix: $[1, 2, 0, 0]^T$.
Dot product: $-1(1) + 0 = -1$.
So 2.4.22 is correct (-1).
Where did 2.4.23 go wrong?
$A_{21}$ in 2.4.23 is $[-1, 0]^T$. $A_{22}$ is row 4.
Wait, Row 2 of Matrix 1 is $[3, 4, 0, 1]$ in 2.4.23?
The problem asks to partition the **first matrix of the previous exercise**.
In Ex 2.4.22, the first matrix is $\begin{bmatrix} 1 & -2 & 0 & 1 \\ -1 & 0 & 0 & 0 \dots \end{bmatrix}$.
In Ex 2.4.23, the matrix shown is $\begin{bmatrix} 1 & -2 & 1 & 0 \\ 3 & 4 & 0 & 1 \dots \end{bmatrix}$.
**These are different matrices!**
The problem text says "Partition the first matrix of the previous exercise as..." but then displays a matrix with different entries (specifically Row 2 and col 3/4 swap).
I must calculate based on the **matrix shown in 2.4.23**, not the values from 2.4.22.
So the result calculated here (11) is correct for the numbers shown in 2.4.23.)