### Answer 3.5.33

**Algorithm:**
$[A \mid I] \to [U \mid L^{-1}?]$.
Reduce A to echelon U. Operations recorded in augmented block.
If rank $r$, last $m-r$ rows of U are 0.
The corresponding rows in the augmented part (matrix M) satisfy $M_i A = 0$.
These rows form basis for $Left\text{-}null(A)$.