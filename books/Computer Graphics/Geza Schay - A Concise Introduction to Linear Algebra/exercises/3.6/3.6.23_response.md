### Answer 3.6.23

**Basis $A$:** (from 3.6.21).
**Basis $B$:** $P_B = \begin{bmatrix} 1 & 0 & 1 & 4 \\ 3 & 4 & 0 & 3 \\ 2 & 4 & 4 & 6 \\ 0 & -1 & 0 & 1 \end{bmatrix}$.

**a. Transition $A \to B$ ($S$):**
$S = P_B^{-1} P_A$. (Compute using MATLAB or Gaussian elimination).

**b. Transition $B \to A$ ($S'$):**
$S' = P_A^{-1} P_B = S^{-1}$.

**c. Coordinates:**
Compute $[\mathbf{x}]_B = S [\mathbf{x}]_A$ using values from 3.6.21.

**d. Representative Matrix $A_B$:**
If $A$ represents a linear operator (ambiguous notation, usually $M$ is the operator), then $M_B = P_B^{-1} M P_B$.
If the question means "find the matrix representing the operator $A$ (as a matrix in standard basis) in basis $B$", then it is $P_B^{-1} A P_B$.

**e. Representative Matrix $B_A$:**
$P_A^{-1} B P_A$ (where $B$ is the matrix in standard basis).