6. Extend the `ApplicationView` in Figure 19.12 to include functionality for setting a world coordinate window bound. The world coordinate window bound defines a rectangular region in the world for displaying in the Viewport. Define a method for setting the world coordinate window bound and modify the `ApplicaitonView::DeviceToWorldXform()` function to support transforming mouse clicks to world coordinate space.
