- **Goal:** Map the top-right-near corner $(r, t, n)$ to the canonical corner $(1, 1, 1)$.

- **Step 1 (Perspective):** $\mathbf{P}$ maps $(r, t, n, 1)$ to $(nr, nt, n^2+fn-fn, n)$.

  - Simplifies to $(nr, nt, n^2, n)$.

  - Homogenize (divide by $n$): $(r, t, n)$.

- **Step 2 (Orthographic):** $\mathbf{M}_{orth}$ maps $(r, t, n)$ to $(1, 1, 1)$ (since $r$ maps to right edge, $t$ to top, $n$ to near).

- **Result:** The point $(r, t, n)$ successfully maps to the normalized device coordinate $(1, 1, 1)$.
