- **Assumption:** An ellipse centered at $(c_x, c_y)$ with width $2a$ and height $2b$.

- **Formula:**

  $$
  \begin{aligned}
  x(t) &= c_x + a \cos t \\
  y(t) &= c_y + b \sin t
  \end{aligned}
  $$

  where $t \in [0, 2\pi)$.
