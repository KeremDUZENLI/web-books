### Answer 5.1.22

**Code:**
```matlab
x = [1; 2; 3]; y = [1; 3; 3];
A = [x, ones(size(x))];
s = A \ y;
m = s(1); c = s(2);
z = polyval(s, x); % Note: polyval expects [m c], A\y returns [m c] if x col is first.
plot(x, y, 'o', x, z, '-');

```

(Calculates the line of best fit for the example points).