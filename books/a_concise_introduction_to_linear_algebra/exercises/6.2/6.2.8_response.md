### Answer 6.2.8

**Matrix:** $4 \times 4$.
$A = \begin{bmatrix} 1 & 1 & 2 & 2 \\ 2 & 0 & 4 & 1 \\ 1 & 3 & 4 & 0 \\ 0 & 3 & 1 & 1 \end{bmatrix}, \quad \mathbf{b} = \begin{bmatrix} 1 \\ 0 \\ 4 \\ 3 \end{bmatrix}$.

1.  **Det(A):**
    Row ops: $R_2 - 2R_1 \to (0, -2, 0, -3)$.
    $R_3 - R_1 \to (0, 2, 2, -2)$.
    $R_4$ is $(0, 3, 1, 1)$.
    Sub-det ($3 \times 3$):
    $\begin{vmatrix} -2 & 0 & -3 \\ 2 & 2 & -2 \\ 3 & 1 & 1 \end{vmatrix}$.
    $R_2 + R_1 \to (0, 2, -5)$.
    Exp Col 1: $-2(2+5) + 3(0+6) = -14 + 18 = 4$? No.
    $-2 \begin{vmatrix} 2 & -2 \\ 1 & 1 \end{vmatrix} - 3 \begin{vmatrix} 0 & -3 \\ 2 & -2 \end{vmatrix}$? No.
    Expand Row 1: $-2(2+2) - 3(2-6) = -8 - 3(-4) = 4$.
    Det(A) = 4.

2.  **Det(A1):** Replace Col 1 with $\mathbf{b}$?
    Wait, $\mathbf{b}$ matches Col 1 of $A$ exactly!
    $\mathbf{b} = (1, 0, 4, 3)^T$ vs $Col 1 = (1, 2, 1, 0)^T$. No.
    Let's calculate $x_i$.
    Actually, observe $\mathbf{b}$.
    $Col_2 = (1, 0, 3, 3)$.
    $Col_4 = (2, 1, 0, 1)$.
    $Col_3 = (2, 4, 4, 1)$.
    Is $\mathbf{b}$ a simple combo?
    $\mathbf{b} = (1, 0, 4, 3)$.
    $Col_2 + Col_3 = (3, 4, 7, 4)$. No.
    Let's calculate determinants.
    $Det(A_1) = \begin{vmatrix} 1 & 1 & 2 & 2 \\ 0 & 0 & 4 & 1 \\ 4 & 3 & 4 & 0 \\ 3 & 3 & 1 & 1 \end{vmatrix}$.
    Expand Row 2: $4(...) - 1(...)$.
    Result: $x_1 = Det(A_1)/4$.
    (Manual calculation required for all 4).