### Answer 4.2.13

**Basis $A$:** Matrix $4 \times 4$ given. Columns are basis for $U$.
**Matrix $[T]$:** $6 \times 4$.
This matrix maps **coordinates relative to A** to standard coords in $\mathbb{R}^6$? Or standard to standard?
Usually $[T]$ means relative to the given basis $A$ and standard basis in range.
Let's assume $[T]$ takes coeffs of $u$ in basis $A$ and produces vector in $\mathbb{R}^6$.
**Range:** Column space of $[T]$.
Matrix has columns. Perform Gaussian elimination on $[T]$ to find Rank.
$[T] = \begin{bmatrix} 0 & 3 & 1 & 0 \\ 4 & 2 & 0 & 1 \\ \dots \end{bmatrix}$.
Use MATLAB/Manual reduction.
If Rank = 4 (Full column rank), Range is 4-dim subspace spanned by cols.
**Kernel:** Null space of $[T]$.
Solve $[T]\mathbf{x} = \mathbf{0}$.
If columns independent, Kernel is $\{\mathbf{0}\}$.
Given the matrix values:
Row 6 is zero.
Row 3 is $(0,0,2,0)$. Implies component 3 is 0.
Row 5: $(2,2,1,0)$. If $x_3=0$, $2x_1+2x_2=0 \implies x_1=-x_2$.
Row 1: $3x_2 = 0 \implies x_2=0 \implies x_1=0$.
Row 2: $4(0)+2(0)+0+x_4 = 0 \implies x_4=0$.
Kernel is $\{\mathbf{0}\}$. T is 1-to-1.
Range is the span of the columns of $[T]$.