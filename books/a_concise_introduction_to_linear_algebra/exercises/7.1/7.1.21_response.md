### Answer 7.1.21

**MATLAB Check (Ex 7.1.8):**
$A$ is block diagonal.
Polynomial: $(\lambda-1)^2 (\lambda-2)^2$.
`roots` returns `2, 2, 1, 1`.
`null(A - 1*eye(4))` returns 2 vectors (e.g., $e_1, e_2$).
`null(A - 2*eye(4))` returns 2 vectors (e.g., $e_3, e_4$).
Matches.